# Final Submission Checklist

- App name is `SiteSpy`.
- Android package is `com.unam.sitespy`.
- Git local identity for integration commit is Washington Mattathias `<matattiasw@gmail.com>`.
- `.env`, secrets, PATs, passwords, and raw collaborator files are not committed.
- Firebase Auth, Firestore, and Storage are enabled.
- Firestore and Storage rules are reviewed and deployed if required.
- `npm run check` passes.
- `npm run doctor` passes.
- `npm run export:android` passes.
- Expo Go Android test is completed.
- EAS preview APK build is completed.
- Student branch ownership is documented in `project-management/work-division.md`.
- Issue drafts are prepared without fake commits or backdated history.
